# Supabase (required for auth, API, and Edge Functions)
# Copy this file to .env and fill in your values
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# JWT tokens are generated by Supabase auth and stored securely on device.
# JWT_SECRET is for server-side only (Edge Functions). Do not add it here.
